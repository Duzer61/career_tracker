services:
  postgres:
    image: postgres:16-alpine
    container_name: career-tracker-postgres
    env_file: .env
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - career-tracker-network

  redis:
    image: redis:7-alpine
    container_name: career-tracker-redis
    env_file: .env
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --requirepass ${REDIS_PASSWORD:-} --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "--pass", "${REDIS_PASSWORD:-}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - career-tracker-network

  career:
    image: duzer61/career_tracker
    env_file: .env
    container_name: career-tracker-career
    ports:
      - "9500:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - career-tracker-network

networks:
  career-tracker-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
